#!/bin/bash

# Usage: ./preprocess input.txt > output.tsv
input_file="$1"

if [ ! -f "$input_file" ]; then
    echo "Error: File not found."
    exit 1
fi

awk -F';' -v OFS='\t' '
NR == 1 {
    print gensub(/;/, "\t", "g", $0)
    next
}
' "$input_file"

